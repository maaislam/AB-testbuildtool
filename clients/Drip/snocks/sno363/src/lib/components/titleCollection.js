const titleCollection = (id) => {
    const htmlStr = `<div class="${id}__collectionHeaderWrapper">
    <h1 class="${id}__collectionHeader">Unsere Empfehlungen für dich</h1>
</div>`;

return htmlStr;
};

export default titleCollection;
