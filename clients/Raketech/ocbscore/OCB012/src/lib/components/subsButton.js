const subsButton = (id) => {
  const html = `
        <div class="${id}__subsButton ocb_web_newsletter" data-attribute="Newsletter">Subscribe to updates</div>
    `;
  return html.trim();
};

export default subsButton;
